= simple_form_for @program, :html => { class: "form-horizontal", role: "form"} do |f|
	= f.input :name, label: "Program Name"
	= f.input :description, label: "Description"

	= f.submit "Save", class: 'btn-large btn-primary'